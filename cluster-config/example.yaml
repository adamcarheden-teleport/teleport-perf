---
tbot:
  clusterName: "my-teleport.example.com"
  teleportProxyAddress: "my-teleport.example.com:443"
  services:
  - type: kubernetes/v2
    selectors:
    - name: my-cluster
    destination:
      type: kubernetes_secret
      name: tperf-tbot
      namespace: teleport-performance
