---
kind: role
version: v8
metadata:
  name: __ROLE__
spec:
  allow:
    # Set labels to match the teleport-joined k8s clusters your bot will run on.
    kubernetes_labels:
      '*': '*'
    # Set the permissions your bot needs below. Both K8S and Teleport
    # permissions are applied independently, so you must also create k8s
    # RoleBindings/ClusterRoleBindings to permit the groups and/or users below
    # equivalent access.
    kubernetes_groups: ["__K8S_GROUP__"]
    kubernetes_users: ["__BOT__"]
    kubernetes_resources:
    # TODO: Implement teleport least priv
    - api_group: '*'
      kind: '*'
      name: '*'
      namespace: '*'
      verbs:
      - '*'
    # Admin rules required to start `tsh proxy kube`
    rules:
    - resources:
      - role
      - kube_cluster
      verbs:
      - read
      - list
#    kubernetes_resources:
#      - kind: selfsubjectrulesreviews
#        api_group: authorization.k8s.io
#        namespace: '*'
#        name: '*'
#        verbs: ["create"]
#      - kind: pods
#        api_group: "*"
#        namespace: "__K8S_NAMESPACE__"
#        name: "*"
#        verbs: ["get", "list"]
#      - kind: pods/exec
#        api_group: "*"
#        namespace: "__K8S_NAMESPACE__"
#        name: "*"
#        verbs: ["create", "get"]
